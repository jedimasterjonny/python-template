# Local guidance

Project-specific rules for Claude Code. This file is **never overwritten** by `just update-template`.

<!-- Add things like:
  - domain vocabulary / glossary
  - architecture quirks the model needs to know
  - "always do X here, never Y"
  - links to internal docs / runbooks
-->
